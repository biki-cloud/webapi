apiVersion: apps/v1
kind: Deployment
metadata:
  name: exec-python2-deploy
  labels:
    app: exec-python2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: exec-python2
  template:
    metadata:
      labels:
        app: exec-python2
    spec:
      containers:
        - name: exec-python2
          image: 192.168.1.12:5010/exec-python:v1.1.0
          workingDir: /root/exec
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: whole-configmap
          env:
            - name: "EXEC_NODEPORT_PORT"
              value: "30012"

# クラスタ外部に公開
# websiteのjavascriptからPOSTを受けつけるため
# apigwからのアクセスも来る
---
apiVersion: v1
kind: Service
metadata:
  name: exec-python2-svc
spec:
  type: NodePort
  selector:
    app: exec-python2
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30012
